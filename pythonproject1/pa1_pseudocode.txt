P1:
# Initialize dimensions of mat 1 and 2 
first_name = "Wade"
last_name = "Poltenovage"
rows = len(last_name)
cols = len(first_name)

Print the name and its length for first and last names

Create method to write matrix to a file

Calculate first matrix:
Fill value of column 1 with digits starting at 0,0  with values starting at 1 and incrementing by +1
Once the columns are filled, iterate rows.

Calculate second matrix: 
Fill value of row 1 with with digits starting at 0,0 with values starting at +2 and incrementing by +3
Once rows are filled, iterate columns.

Write mat1 to file

Calculate second matrix: 
Fill value of row 1 with with digits starting at 0,0 with values starting at +2 and incrementing by +3
Once rows are filled, iterate columns.

Write mat2 to file

Calculate third matrix: 
Initialize the number of rows at 2
Initialize the number of columns at 4
Fill value of row 1 with with digits starting at 0,0 with values starting at 10 and decrementing by -2
Once rows are filled, iterate columns.

Write mat3 to file

Calculate 4th matrix: 
Initialize the number of rows at 4
Initialize the number of columns at 2
Fill value of row 1 with with digits starting at 0,0 with values starting at -6 and incrementing by +1.5
Once rows are filled, iterate columns.

Write mat3 to file

Print matrices are printed

P2:
Define Functions
Define read from file
Define write to file
Define add matrices
Define matrices have same dimensions

Request name of matrixA
Request name of matrix

Find files

Call read file for matrices
Call add file for matrices

Write merged matrices to new file named wpoltenovage_p2_out12.txt as it is the only compatible files


Print confirmed matrix addition and print new file name


P3:
Import numpy

Define Functions:
Define read from file using numpy
Define write to file using numpy
Define add matrices using numpy
Define matrices have same dimensions using numpy

Request name of matrixA
Request name of matrix

Find files

Call read file for matrices
Call add file for matrices

Write merged matrices to new file named wpoltenovage_p3_out12.txt as it is the only compatible files


Print confirmed matrix addition and print new file name

